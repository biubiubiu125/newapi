# NewAPI 上游同步分类报告

日期：2026-09-03

## 只读基线

- 工作区：`C:\Users\Administrator\codex-1\newapi`
- 当前分支：`main`
- 本地提交：`d7528a12363a2a28fb421347b60ecfc7db7cb686`
- 上游提交：`0ed497f066a68613375124303ef54f220267b334`
- 公共祖先：`823e26304a396854ace30b52b98ec497c2dd9c36`
- 上游仅有提交：70 个；双向变更文件：691 个。
- 本次分类未执行 merge、rebase、cherry-pick、reset 或远端推送。

## 本地保护边界

按当前代码和调用链保护：图片任务创建、幂等、排队、执行、结果存储、下载、取消、退款/结算与租约；支付、充值、订阅、推荐、提现；渠道故障转移；计费、用量和权限/状态；部署配置；正在使用的新版 `web/` 前端。`web/classic/` 已确认不再参与当前构建、嵌入、路由、CI 和发布链路，归入废弃代码清理。

## 分类结果

### 1. delete obsolete（删除废弃代码）

- 本地 `web/classic/` 源码及其残余资源。
- classic 的构建、Embed、CI、release、路由和主题切换分支。
- 旧 `theme.frontend` 的运行时切换不再保留；数据库迁移/兼容读取暂时保留，防止已有配置失效。

### 2. absorb generic fix（直接吸收通用修复）

这些改动不应改变本地商业业务语义，按上游实现核对后吸收：

- `b7017c251`：系统任务状态无变化时不误判锁丢失。
- `27ff6a876`、`2b6f1dfef`、`69a41eead`、`74158715c`：旧约束清理、初始化和迁移兼容。
- `8c8c4153d`：usage 统计保留 quota。
- `b518d0033`：限制等待上游响应头，防止无界内存增长。
- `6eb6f35ed`、`66031a09d`、`1751f43ee`：PostgreSQL/SQLite 连接和并发兼容。
- `0f2a2075a`、`d49160f0e`：请求/后端长度校验和正确 HTTP 状态码。
- `7037ac15b`、`8c25eee71` 及与当前构建配置不冲突的依赖/构建修复。
- `67a0585d0` 及不涉及二开接口的文档链接修正。
- 前端 setup 状态刷新、凭证自动填充防护、移动侧栏、搜索去抖、token 刷新和安全确认等通用前端修复。

### 3. manual fusion（手工融合）

以下改动与本地二开存在交集，禁止整文件替换：

- `0ed497f06`：hosted-tool 转换、reasoning 规范化、usage/计费完整性；必须沿 Relay → 预扣 → 结算 → 日志链路融合。
- `dc4732cfe`、`eb48396d5`、`6c22550ea`：任务插件系统、插件模型别名和运行时；保留本地图片任务状态与结算链路。
- `ba2e9287b`、`cae3676ec`、`8f6961c67`、`bb234ff41`、`253a74dd1`、`7d09c6954`、`3dda1d50c`、`4442bb302`、`8ad159a3b`：Relay/provider 请求转换、Responses/Claude/Ollama/vLLM 语义。
- `ac381acf4`、`58d4e9bd3`、`ccd535ef8`、`50e5377ea`、`47ba9d2c6`、`2a0ce3475`、`f11641428`：计费、充值、退款、并发扣费和缓存 token 结算。
- `918427d8a`、`b80d633cf`、`d7992672a`：密码传输/加密和 OAuth 用户状态；要保留现有认证兼容和配置约定。
- `4add708eb`、`5d3423bec`、`2b8e50bf3`、`e90a7c48e`：渠道测试、路由编辑、字段透传；要保留本地渠道故障转移和管理权限。
- `b5b94bc68`、`692e8d6ee`、`15cfdedde`、`98d50d538`、`137d1171f`、`4eaeefbdf`、`7dd1000a1`：前端交互与后台设置，按当前新版 `web/` 组件融合。

### 4. hold for maintainer review（保留待维护者复核）

- 上游完整替换内置任务适配器为沙箱 JS 插件系统，若会改变现有图片任务生产协议、部署要求或任务结算语义，不能自动替换。
- 大规模插件协议/API、视频代理重构、认证加密默认值改变、`int32` 全局退役和依赖大版本变化，需要分别验证兼容性后再决定。
- 上游 README、PR 模板、第三方许可证和整套前端 lockfile 变更不作为业务同步完成条件；仅采纳与本地发布直接相关且已验证的部分。

## 执行顺序

1. 完成 classic 废弃代码清理和兼容文档。
2. 完成任务产物公开地址的真实链路接入；若发现已有等价链路，则合并而不是重复实现。
3. 先吸收数据库/运行时/请求校验等低风险修复。
4. 手工融合 Relay、usage、计费、充值/退款和任务插件相关改动。
5. 融合新版前端和构建依赖，逐组运行测试。
6. 对剩余高风险变更保留明确复核记录，不把“本地同步完成”描述成“已完整合并上游”。

## 当前结论

这是行为级同步，不是把本地分支直接推进到 `upstream/main`。截至本报告生成时，未发生上游合并；现有代码改动仍是未提交的第一阶段实现。
